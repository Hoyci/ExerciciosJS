function divisão(dividendo, divisor) {
    var resultado = (dividendo / divisor)
    var sobra = dividendo % divisor
    console.log(resultado.toFixed(2), sobra.toFixed(2))
}

divisão(10, 2)
divisão(10, 3) 